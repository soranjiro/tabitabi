-- migrate:up
-- This migration was recorded as applied before its SQL because the original
-- file placed the down marker above the schema changes. The actual repair is
-- intentionally owned by 20260815100000_repair_shared_money_fund.sql.
SELECT 1;

-- migrate:down
SELECT 1;
